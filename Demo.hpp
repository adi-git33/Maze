#pragma once
#include "Maze2d.hpp"
#include <iostream>
#include <string>
#include <memory>
#include <map>
#include <utility>
#include <exception>
#include <vector>
#include <iterator>
#include <fstream>
#include <cstring>

using namespace std;
class Demo
{
private:
public:
    // Constractors
    Demo();
    // Destractors
    virtual ~Demo();
    // Operators
    // Getters
    // Setters
    // Functions
    void Run(){
        // Creates maze
        // prints maze
        // sovles with bfs
        // solves with astar with each h func (runs twice)
        // display how many state every alg solved
    };
};